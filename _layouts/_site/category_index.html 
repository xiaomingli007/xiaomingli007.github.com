{% include header.html %} 
<div class="container">
	<div class="row-fluid">
		<div class="span12">&nbsp;</div>
	</div>
	<div class="row-fluid">
		<div class="span12">
			<h1 class="site-title"><a href="/">{{ site.name }}</a></h1>
		</div>
	</div>
	<div class="row-fluid">
			<div class="span9">
				<div class="tag-title">{{page.title}}</div>
				<h2>Category:&nbsp;&nbsp{{ page.category }}</h2>
				{% for post in site.categories.[page.category] %}
					<p class="post-meta">{{ post.date | date: '%Y' }}</p>
					<h3><a href="{{ post.url }}">{{ post.title }}</a></h3>
					<p class="post-content">{{ post.summary }}...</p>
				{% endfor %}     
				
			</div>
			<div class="span3">
				{% include sidebar.html %}
			</div>
		</div>
</div>

{% include footer.html %} 



